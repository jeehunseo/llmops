# Langfuse API keys for this project, created via LANGFUSE_INIT_PROJECT_* in
# ../langfuse/.env (auto-provisioned on first startup), or manually at
# http://localhost:3001 -> Settings -> API Keys.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
